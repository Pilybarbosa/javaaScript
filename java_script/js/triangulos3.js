function calcular(i) {
    i.preventDefault()
    btn.onclick =calcular= calcular(i)
        i.preventDefault()

    //entrada
    let pp=document.getElementById("pp").value
    do {
        if (p==op && op==oop && opp==p) {
            
            alert(`es un triangulo equilatero `)
        } else
        if (op==p || oop==op || p==oop) {
            alert(`utt tiene un triangulo isoceles`)
        } else{
        
            alert(`utt tiene un triangulo escaleno`)
            
        }
    } while (pp);
        alert(`desea volver a jugar `)
    
    }
